using UnityEngine;
using System.Collections;

public class BG_TunnelRunner_Menu : GameState {

	// Use this for initialization
	void Start () {
        //When the timer reaches 0 the player will lose
        TimerWin();
    }

    // Update is called once per frame
    void Update () {
	
	}
}
